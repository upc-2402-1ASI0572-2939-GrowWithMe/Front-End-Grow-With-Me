import { ConsultantsService } from './consultants.service';

describe('ConsultantService', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ConsultantsService()).toBeTruthy();
  });
});
